# LibTester

Tests external libraries so we can fix bugs.

## Commands
* `/libtest <library_name>`- Executes the sandbox test routine.
**Example:** `/libtest shared`

## Usage
Use this diagnostic tool to verify engine stability during addon load/unload cycles, command unregistration, and dependency checks.